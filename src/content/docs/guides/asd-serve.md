---
title: HTTP & Lens (asd-serve)
description: HTTP server plus the Lens review UI for browsing the ledger.
---

`asd-serve` exposes the same primitives over HTTP and ships the **Lens** review UI — a browser-based view of symbols, ledger entries, effects, and the call graph. Useful for human reviewers and for embedding in code-review tooling.
